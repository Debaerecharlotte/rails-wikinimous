<div class="show-page">

  <div class="article-actions">
    <h3><%= @article.title %></h3>
    <%= link_to 'Edit', article_edit_path(@article) %>
    <%= link_to 'Bin', article_path(@article), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this masterpiece?"} %>
  </div>

  <p><%= @article.content %></p>

</div>
